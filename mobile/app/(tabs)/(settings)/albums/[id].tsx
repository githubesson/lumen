// Same album detail screen as the Library tab, registered in the Settings
// stack so Replay can push it locally — back returns to Replay instead of
// jumping to the Library tab.
export { default } from "../../(library)/albums/[id]";
