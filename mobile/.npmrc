# reanimated nightly (^4.6.0-nightly-*) is a prerelease and never satisfies
# react-native-reorderable-list's peer range ">=3.12.0" under strict semver.
# Required for `npm ci` on EAS to succeed.
legacy-peer-deps=true
